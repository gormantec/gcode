const assert = require("assert");
const myModule = require("../../near-api-as");

console.log("-----------------------");

var     sender,
publicKey,
receiver,
nonce,
actions,
recentBlockHash;

console.log(myModule.createTransaction());




console.log("ok");
