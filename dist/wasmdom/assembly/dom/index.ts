import { Debug, Document, Console, Window, URLSearchParams,Promise,Response,fetch, setTimeout } from "./document"
import { Element,Callback,EventCallback } from "./element"

export { Debug, Document, Console, Window, URLSearchParams,Promise,Response,fetch, setTimeout,Element,Callback,EventCallback };