name: gmobile build
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: echo
      run: |
        echo ${{ secrets.WEBHOOK_URL }}
        echo ${{ secrets.TOKEN }}
        curl \
        -v -H "Authorization: token caf82def664bfc8049a11eaff8ee977a9ad9a379" \
        -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        https://api.github.com/repos/gormantec/gmobile/actions/workflows/workflow_dispatch.yml/dispatches \
        -d '{"ref":"master"}'
 #   - name: Webhook
 #     uses: zzzze/webhook-trigger@master
 #     with:
 #       data: "{\"event_type\":\"build\"}"
 #       webhook_url: ${{ secrets.WEBHOOK_URL }}
 #       options: "-H \"Accept: application/vnd.github.everest-preview+json\" -H \"Authorization: token ${{ secrets.TOKEN }}\""
